/**
 * Configuración de entorno para Docker
 * Este archivo es generado/reemplazado por el script docker-entrypoint.sh
 * 
 * En desarrollo: Este archivo puede estar vacío o no existir
 * En producción (Docker): Se genera con las variables de entorno del contenedor
 */

window.__ENV__ = {
    // Estas variables serán reemplazadas por el script de Docker
    // API_URL: "${API_URL}",
    // APP_NAME: "${APP_NAME}",
    // FIREBASE_API_KEY: "${FIREBASE_API_KEY}",
    // ... etc
};

